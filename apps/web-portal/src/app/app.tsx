/**
 * Customer Portal shell (Task 18): brand placeholder only, no routing yet — the
 * Customer Portal BFF and its feature libs land in a later task (ADR-008 — apps
 * compose feature libs, never own business logic).
 */
export function App() {
  return (
    <div>
      <h1>Helm Customer Portal</h1>
    </div>
  );
}

export default App;
