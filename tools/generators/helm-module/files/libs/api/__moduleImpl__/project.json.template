{
  "name": "<%= schema %>",
  "projectType": "library",
  "sourceRoot": "libs/api/Helm.<%= name %>",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "dotnet build libs/api/Helm.<%= name %>/Helm.<%= name %>.csproj" } },
    "test":  { "executor": "nx:run-commands", "options": { "command": "dotnet test libs/api/Helm.<%= name %>.Tests/Helm.<%= name %>.Tests.csproj" } }
  },
  "implicitDependencies": ["core", "<%= schema %>-contracts"]
}
